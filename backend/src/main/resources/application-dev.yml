spring:
  jpa:
    show-sql: true
  devtools:
    restart:
      enabled: true

jwt:
  secret: dev-secret-key-min-256-bits-long-for-hs256-algorithm

logging:
  level:
    com.dhairya: DEBUG
    org.springframework.security: DEBUG
